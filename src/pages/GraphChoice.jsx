import React from 'react';
import { Button, Container, Row, Col } from 'react-bootstrap';

const GraphChoice = () => {
	return (
		<Container>
			<h1>Please Choose a Graphs you want to use</h1>
		</Container>
	);
};

export default GraphChoice;
